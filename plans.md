# 動的仕様書

## Goal
画像を縦方向に4等分し、分割位置の上下6pxを削除した後、分割画像を表示・1クリックでダウンロードできるWebアプリを作成し、GitHub Pages で公開する。
分割境界線をドラッグで調整できる機能を追加。

## Spec

### 機能要件
1. **画像アップロード**
   - ユーザーが画像ファイルを選択できる
   - 画像入力は最下部に配置
   - サポート形式: PNG, JPG, JPEG, WEBP

2. **画像分割処理**
   - 画像を縦方向に4等分する（デフォルト）
   - 境界線をドラッグで調整可能
   - 各分割位置で上下6pxを削除する（マージン調整）
   - Canvas API を使用してクライアントサイドで処理
   - 境界線調整時にリアルタイムで分割画像を更新

3. **分割画像表示**
   - Canvas上に分割境界線を表示
   - 分割後の4枚の画像をグリッド表示
   - 各画像にサイズ情報を表示

4. **ダウンロード機能**
   - 各分割画像を1クリックでダウンロード
   - ファイル名: `original_{index}.jpg` 形式（元画像名にサフィックス）
   - 出力形式: JPEG (品質95%)

### 非機能要件
- 純粋なフロントエンド実装（Node.js不要）
- GitHub Pages でホスティング可能
- モダンで美しいUI
- レスポンシブデザイン

## Tasks

### Phase 1: 構想・着手
- [x] 要件を確認
- [x] 動的仕様書を作成
- [x] index.html の実装構想を決定

### Phase 2: 実装
- [x] index.html を作成（HTML構造）
- [x] CSSスタイルを実装
- [x] JavaScriptで画像分割ロジックを実装
- [x] ダウンロード機能を実装

### Phase 3: 検証
- [x] ブラウザで動作確認
- [x] 複数の画像形式でテスト
- [x] レスポンシブ動作を確認

### Phase 4: 完成
- [ ] GitHub Pages 用の設定（必要であれば）

## Decisions
- Canvas API を使用してクライアントサイドで画像処理を行う（サーバーレス実装）
- 純粋なHTML/CSS/JSで実装し、ビルドプロセスを必要としない

## Observations
- 分割位置の上下6px削除: 各分割画像の境界でオーバーラップやマージン調整を行う
- 4等分計算: height/4 で分割
- マージン: 各分割画像で上下6pxずつ削除 = 実質的な分割位置からのオフセット
